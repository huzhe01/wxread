# 微信读书自动化常见问题 (Troubleshooting)

## ❌ 错误：Cookie 已失效 (IP变动或过期)

如果你收到了这个通知，说明你的登录状态（Cookie）已经过期，或者因为 IP 变动被微信风控系统暂时踢出。

### 根本原因
微信读书的安全机制比较严格。GitHub Actions 的运行环境是在云端（Azure/AWS），每次运行时分配的 IP 地址都是随机的（而且可能是跨国的）。
*   **昨天运行 IP**: 1.2.3.4 (美国)
*   **今天运行 IP**: 5.6.7.8 (日本)
这种剧烈的 IP 变动会让微信认为账号异常，从而强制要求重新登录。

### 解决方案

#### 方案 A：手动更新（当前默认）
既然无法避免 IP 变动，最简单的办法就是**人工接力**。
1.  本地运行 `python src/tasks/wxread_login.py` 扫码登录。
2.  把生成的 `state.json` 内容更新到 GitHub Secrets (`WXREAD_STATE`)。
3.  脚本会自动尝试维持这个 Cookie，直到下一次被风控（通常能坚持 1-2 周）。

#### 方案 B：本地运行（推荐长期稳定）
如果你有一台常开的设备（如 Mac mini, 树莓派, NAS），在本地运行是最稳定的，因为 IP 固定（或是家宽 IP），几乎不会掉登录。
*   配置 `crontab` 每天定时运行 `src/tasks/wxread.py` 即可。

#### 方案 C：配置固定 IP 代理 (高级)
如果在 GitHub Actions 上运行，可以使用代理服务让出口 IP 固定。
1.  购买其它的代理服务。
2.  在脚本中配置 Playwright 使用代理：
    ```python
    browser = p.chromium.launch(proxy={"server": "http://my-proxy:8080"})
    ```
这需要额外的成本。

---

## 调试方法
脚本运行时会生成截图 (如果开启了调试模式)。
*   `debug_entered_page.png`: 进入页面时的截图。
*   如果不确定是否登录成功，可以查看此截图：
    *   看到**二维码/登录框** -> 未登录。
    *   看到**头像/书籍内容** -> 登录成功。
